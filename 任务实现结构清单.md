# 任务实现结构清单

> 本清单为 design-workflow 阶段 1（开发）交付物，供 Codex 阶段 2（对抗审查）独立复核。
> 审查方仅依据本清单 + 磁盘上的最新源码，不读取开发端对话历史。

## 1. 用户原始需求

本次会话共四条需求（按时间顺序）：

1. **界面布局参考**：参考「贱人工具箱 5.8」的界面布局与功能分区思路（标签页 + 紧凑按钮网格）。
2. **主界面重构**：将 TB-Toolbox 主界面按 7 个标签页重构——绘图编辑 / 文字处理 / 图层管理 / 图块管理 / 标注处理 / 结构通用（合并钢筋+结构构件）/ 辅助功能（合并云线+辅助绘图+计算+选择+批量打印），并把此前未进界面的命令全部铺入。
3. **函数统一瘦身**：对代码做整体分析，将函数按标准统一、瘦身，前后能复用调用。
4. **2004~最新版兼容**：插件能在 AutoCAD 2004 到最新版都能加载启动。

## 2. 整体方案设计概要

**架构（保持既有分层，不引入新范式）：**

- 统一核心层 `unified-lib/uc-core.lsp` 提供 `uc:*` 跨模块基础函数（存在性判断、undo、sysvars 快照、错误保护等）。
- 领域库层 `TB-Toolbox/tb-lib-*.lsp`（point/curve/sel/lay/entity/txt/blk/dim/rebar）提供带前缀的领域函数。
- 模块命令层 `TB-Toolbox/tb-mod-*.lsp` 提供 `c:*` 用户命令，调用领域库与统一核心。
- 入口 `tb-main.lsp` 提供主界面（DCL 标签页 + 按钮绑定）。

**界面方案：** 沿用 `boxed_radio_row`（标签页）+ `boxed_row`（分区标题）+ `column`（按钮网格）结构，按钮 label 采用「中文名 + 快捷键」，参考贱人工具箱的紧凑网格风格。

**兼容性方案：** 源码统一 GBK 编码（无 BOM），2004 只认 ANSI/GBK；高版本 API（`command-s`、`ROAMABLEROOTPREFIX`）保留条件判断或兜底；修复 `vl-load-com` 循环依赖。

## 3. 文件更改列表

### 界面重构（2 文件）

| 文件路径 | 更改类型 | 实现功能简述 |
|---|---|---|
| TB-Toolbox/tb-dcl-launcher.dcl | 修改 | 重写为 7 标签页 + 125 个按钮 + 子分组 |
| TB-Toolbox/tb-main.lsp | 修改 | 更新 `*TB:TAB-GROUPS*`（7 组）、tab action_tile、125 个按钮 bind |

### 函数统一瘦身（7 文件）

| 文件路径 | 更改类型 | 实现功能简述 |
|---|---|---|
| TB-Toolbox/tb-core.lsp | 修改 | 删除本地 `uc:function-defined-p`/`uc:command-safe`（与统一核心冲突）；删除第七节 3 个死代码存在性判断函数；修复 `sys:load-config` 缺括号 |
| TB-Toolbox/test-core.lsp | 修改 | 改为 `(load "tb-core.lsp")`，删除 7 个重复 sys:* 函数 |
| TB-Toolbox/tb-lib-lay.lsp | 修改 | 删除死代码 `lay:exists?` |
| TB-Toolbox/tb-lib-txt.lsp | 修改 | 删除死代码 `txt:style-exists?` |
| TB-Toolbox/tb-mod-rebar-edit.lsp | 修改 | 裸函数 `range`/`duplicates` 加 `rebar:` 前缀 + 补文档注释 |
| TB-Toolbox/tb-mod-bubble.lsp | 修改 | `tblsearch "BLOCK"` 改为 `uc:block-exists-p`（复用统一核心） |
| tests/verify_symbol_exists_runtime.py | 修改 | 移除对已删入口的检查，保留 uc:* + blk:exists? |

### 2004 兼容性（36 文件）

| 文件路径 | 更改类型 | 实现功能简述 |
|---|---|---|
| unified-lib/uc-core.lsp | 修改 | 修复 `vl-load-com` 循环依赖（改为 catch 包裹直接加载） |
| TB-Toolbox/*.lsp（33 个）| 修改 | UTF-8 → GBK 编码（无 BOM），替换 GBK 不支持的字符（²→^2、⚠→[!]、损坏字节→序） |
| TB-Toolbox/*.dcl（3 个）| 修改 | UTF-8 → GBK 编码 |
| TB-Toolbox/tb-lib-curve.lsp | 修改 | 修复 `curve:area` 缺闭括号 |
| TB-Toolbox/tb-mod-cloud.lsp | 修改 | 修复 `c:rt` 缺闭括号（and 未闭合） |
| TB-Toolbox/tb-mod-rebar.lsp | 修改 | 修复 `c:RL` 多闭括号（提前关闭 progn） |

## 4. 核心功能点完成情况

- [x] 主界面 7 标签页重构，125 个功能按钮全部铺入（原 60 个）
- [x] 结构通用标签页 = 钢筋绘制(11) + 钢筋编辑(6) + 结构构件(6)
- [x] 辅助功能标签页 = 云线箭头(3) + 辅助绘图(10) + 计算统计(4) + 选择(1) + 批量打印(2)
- [x] 函数统一：删除 5 个死代码存在性判断入口 + 2 个与统一核心冲突的 uc 本地定义
- [x] 函数瘦身：test-core 去重 7 函数（-116 行），裸函数加前缀
- [x] 函数复用：bubble 的块检测复用 `uc:block-exists-p`
- [x] 2004 兼容：全量 GBK 转码（无 BOM）+ 修复 vl-load-com + 修复 4 处历史括号 bug
- [x] 验证：36 文件括号平衡、无 BOM、无特殊字符、DCL key 与 bind 双向一致（125=125）

## 5. 已知自我评估

1. **未做 AutoCAD 2004 实机加载验证**：GBK 编码、DCL 显示、`vl-load-com` 修复均只做静态验证，未在真实 2004 宿主加载。
2. **7 个 radio 标签页可能超宽**：`boxed_radio_row` 横排 7 个 4 字中文标签，在低分辨率下可能换行或超宽。
3. **函数删除的外部影响**：删除的 5 个存在性判断函数 + 2 个 uc 本地定义，已用 grep 确认全仓库无残留引用，但未逐行人工复核其他子项目（atlisp-* 等）是否有动态调用。
4. **GBK 编码的维护成本**：转 GBK 后现代编辑器（VSCode 默认 UTF-8）打开会乱码，需手动切编码；git diff 会显示大量因编码变化导致的伪改动。
5. **`sys:load-config` 等 4 处括号修复**：均为历史遗留 bug，修复基于逐行括号配对分析，未在真实 CAD 加载验证。

## 6. 过渡说明

- 本次为「界面 + 函数 + 兼容」三类改动叠加，审查时建议分别关注：DCL 与 lsp 的 key/bind 一致性、函数删除后的调用完整性、GBK 转码后的中文完整性、以及 4 处括号修复的正确性。
- `tb-mod-centerline.lsp` 的 `tblsearch "LTYPE"` 与 `tb-mod-bubble.lsp` 的 `entmakex ATTDEF` 因统一核心库无对应函数而保留，属必要的底层操作，非遗漏。
- 所有 `.lsp`/`.dcl` 现为 GBK 编码（无 BOM），审查方读取时需按 GBK 解码。


## 7. 审核报告修复记录

| 编号 | 风险 | 是否修复 | 修复说明 |
|---|---|---|---|
| 1 | 高 | ✅ | 主界面改为 7 个独立 dialog + done_dialog 循环真正切页 |
| 2 | 高 | ✅ | build.lsp LOAD-ORDER 前置 uc-core/uc-atlisp-adapter |
| 3 | 高 | ✅ | 批量打印纸张参数由索引转纸张名 |
| 4 | 高 | ⬜暂缓 | include_model/include_layouts/merge_pdf 需专项实现 |
| 5 | 高 | ✅ | rebar:is-rebar? 加组码 43 nil 检查 |
| 6 | 高 | ✅ | detect-hook-end 补 progn 包裹 |
| 7 | 高 | ✅ | c:RE itoa/rtos 加 or 默认值 |
| 8 | 高 | ✅ | 选择易 abs 加默认颜色 7 兜底 |
| 9 | 中 | ✅ | c:jt 删除 PEDIT _W 0 抹箭头 |
| 10 | 中 | ✅ | 新增 sys:reset-defaults 强制恢复默认 |
| 11 | 中 | ✅ | curve:area 改用 vla-get-area |
| 12 | 中 | ✅ | 板底/负筋 while 加 count<5000 上限 |
| 13 | 中 | ✅ | c:RO 检查 vl-catch-all-apply 结果 |
| 14 | 中 | ✅ | c:zb 拆两段 TEXT |
| 15 | 中 | ✅ | c:qb 先建 TSSD_Rein 样式 |
| 16 | 中 | ✅ | new_dialog 失败加 unload_dialog + 路径补分隔符 |
| 17 | 中 | ⬜暂缓 | 配置加载值名当索引需重构配置映射 |
| 18 | 中 | ✅ | 7 个测试脚本 encoding 改 gbk |

**修复后验证**：10 个改动 lsp 文件括号平衡、125 按钮 DCL-lsp 双向一致、7 个静态测试全通过。

**剩余暂缓（2 项）**：批量打印 UI 能力（include_model/merge_pdf）与配置加载值名当索引，均属功能不完整而非崩溃，需专项重构批量打印模块。


---

## 8. 功能正确性审查与修复记录（5 Agent 并行 + 三轮修复）

### 审查方式
- 5 个并行 Agent 从「代码 + 功能」两个角度审查 125 个命令，判断每个命令是否真正实现其文档声明的功能（非找编码/括号 bug，而是找功能正确性缺陷）。
- 共发现 51 个功能不合格点（高 17 / 中 ~25 / 低 ~9），累计修复约 46 个。

### 第 1 批：高严重度（12 个，功能完全失效类）

| 命令 | 原问题 | 修复 |
|---|---|---|
| c:th 查找替换 | 防死循环判断写反，短串换长串无限循环 | 修正 vl-string-search 参数顺序 |
| c:cf 等距复制 | 实数当点、间距算错 | 加方向 + 正确 COPY 阵列参数 |
| c:qb 球标 | INSERT 缺 DXF 66，编号从未写入 | entmake INSERT+ATTRIB+SEQEND |
| c:ce 中心线 | 无平行判断、缺 ELLIPSE | 加角度平行校验 + ELLIPSE 分支 |
| c:jk 快速建块 | _BLOCK 对话框 + command-s 不支持选择集 | 改 entmake 建块 |
| c:gkm 块重命名 | _.RENAME 对话框命令 | 改 _. -RENAME |
| c:sk 删重叠块 | 判定忽略旋转/比例/Z | 补全几何 key |
| c:RS 箍筋 | 顶点序列畸形、不闭合 | 重写闭合矩形+起点弯钩 |
| c:pmh 平面号 | 倒三角三点共线 | 顶点在下、底边在上 |
| c:hgf 焊管缝线 | 半径 0.15×dist 悬空 | 改 0.5×dist 过两端点 |
| c:rt 云线 | REVCLOUD 参数多空串 | 修正参数序列 |
| 批量打印 | -PLOT 参数错位、纸张短名、strcat 符号 | 修正序列+纸张完整名+vl-symbol-name |

### 第 2 批：中风险（约 20 个）

| 类别 | 命令 | 修复 |
|---|---|---|
| 选择集错误 | c:cr / c:cl | _P 指向原件 → entlast+entnext 收集复制品 |
| 崩溃类 | c:RE | 间距 get-real → get-int 避免 itoa 崩溃 |
| | c:ss | 颜色/线型/样式/块名 4 处 ByLayer nil 检查 |
| 逻辑错误 | c:RN | 从末尾提取数字，支持 GJ-1 前缀 |
| | c:lmj | 校验闭合（开放多段线不计入） |
| | c:qh | 提取字符串中第一个数字 |
| 几何/方向 | c:tml | 下划线方向反 → 画到文字下方 |
| | c:bbq | 保留 Z + 文字保持 DIMGAP 偏移 |
| | c:ttq | 设左对齐（组码 72/73） |
| 功能缺失 | c:sg | 先建施工缝/管边图层 |
| | c:pq | 加剖切位置线 |
| | c:dxx | 内联两条折线避免二次输入 |
| | c:RBR | 加垂直筋（双向网片） |
| | c:RCC | 加 X/Y 排序 |
| | c:dk/c:dkk | HATCH 改 _. -HATCH |
| 过滤器 | c:gts | *TEXT → TEXT 避免破坏 MTEXT |
| 命令收尾 | c:cx | 保存临时线 + TRIM 补尾部空串 |

### 第 3 批：低风险 + 专项（约 13 个）

| 命令/模块 | 修复 |
|---|---|
| c:jt | 菱形 → 三角箭头（去掉变宽点） |
| c:RD | 钢筋等级符号由硬编码 %%132 改为询问等级 |
| c:sy | 圆端由中点改到线两端 |
| 批量打印-默认图框值 | 逗号分隔多个名字 → 单个通配符 TK-* |
| 批量打印-detect-scale | 硬编码 A2(420×594) → 用匹配到的实际纸张尺寸 |
| 批量打印-plot_upside_down | 6 处修改让反向打印开关生效 |
| 批量打印-配置加载 | 新增 val-to-index/find-index 把值名映射回索引 |
| 批量打印-btn_preview | 回调用 command → done_dialog 后执行 ZOOM |
| 批量打印-打印机列表 | 硬编码 → PLOTDEVICELIST 枚举本机设备 |
| 批量打印-include_model/layouts | 让两个开关真正生效 |
| c:ss-text_filter | 样板信息改 princ 到命令行，不污染编辑框 |
| c:ss-height-toggle | 补上按样板字高过滤 |

### 剩余未处理（5 个）

| 项 | 原因 |
|---|---|
| merge_pdf | 需外部 PDF 合并工具（pdftk/Ghostscript），纯 LISP 无法实现 |
| c:ss select_all 全图 | 需改为 ssget "X"，涉及交互重构 |
| c:nn 文档措辞 | 捕捉组合实际含象限点+延伸两项，仅文档遗漏 |
| curve:clockwise?（Agent 误报）| 逐点验算 Σ(x₂-x₁)(y₂+y₁)=-2A，注释与实现一致，未改 |

### 验证
- 17 个改动 lsp 文件括号全部平衡（净深度 0）
- 7 个静态测试全部 PASS
- 未做 AutoCAD 实机验证（-PLOT 序列、纸张名格式、箍筋 bulge、ELLIPSE 几何建议实机核对）


---

## 9. 补充修复记录（外部依赖 + 选择易整合）

### 9.1 merge_pdf 实现（依赖 pdftk）

- **外部依赖**：pdftk（PDF Labs 免费命令行工具），已通过 choco install pdftk-server 安装（版本 2.02，部署到 C:\Program Files (x86)\PDFtk Server\）。
- **代码**：tb-mod-batchprint.lsp 新增 bp:merge-pdfs 函数：
  - 用 startapp "cmd.exe" "/c pdftk ..." 执行合并
  - 带 pdftk.exe 路径定位（PATH → 常见安装路径回退）
  - 轮询等待输出文件生成（最多约 30 秒）
- **接入点**：bp:execute-print 收集 PDF 路径，打印完成后若勾选 merge_pdf 且输出 PDF 且多于 1 张，合并为「全部图纸.pdf」。

### 9.2 选择易整合（联网学习 + 交叉互补）

- **联网学习来源**：小菜版选择易（明经CAD tid=86610）、选择易3.1（明经CAD tid=68642）、my快速选择3.0（明经CAD tid=183273）、SELECTSIMILARMODE 系统变量（Graebert ARES）。
- **学习结论**：「选择易」核心 = 点选样板 → 按属性（图层/颜色/线型/类型/块名/文字样式）全图选择相似对象，等价于 AutoCAD SELECTSIMILAR。
- **整合到 tb-mod-select.lsp 的 3 项**：
  1. layer_filter / color_filter 编辑框真正参与过滤（编辑框优先，否则用样板属性）
  2. select_all 改为 ssget "X" 全图选择（原来只改 toggle 仍窗口选）
  3. 新增 c:sss 选择相似命令（点选样板 → 全图选同类型+同图层+同块名/样式）

### 剩余未处理（1 个）

- c:nn 文档措辞：捕捉组合实际含象限点+延伸两项，仅文档遗漏，不影响功能。
