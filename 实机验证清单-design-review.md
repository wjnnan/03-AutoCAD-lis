# 实机验证清单 — design-review 改动

> 2026-08-16 design-review 会话改动（P1 6 项 + P2 5 项）的实机验证清单。
> 前置会话改动（批量打印 ActiveX 重写、87 命令守卫）的 P0 验证项见「当前任务详情与后续工作清单.md」，此处不重复。
> 本次改动文件：tb-dcl-launcher.dcl、tb-dcl-batchprint.dcl、tb-dcl-setting.dcl、tb-mod-batchprint.lsp、tb-main.lsp。

---

## 〇、前置准备

| 项 | 操作 | 预期 | 失败时处理 |
|---|---|---|---|
| 加载 | `(load "TB-Toolbox/load.lsp")` 或按 build 流程 | 无加载错误 | 检查 GBK 编码是否有文件被误转 UTF-8 |
| DCL 可加载 | 依次打开主界面/批量打印/设置，任一对话框能弹出即代表对应 DCL 语法通过 | 三个对话框都能 `new_dialog` 成功 | 命令行会报 `Unable to load dialog file` 或具体行；检查改动区标点 |
| 版本矩阵 | **AutoCAD 2004 与最新版各测一遍**（项目支持 2004） | 行为一致 | 记录版本差异 |

---

## 一、主界面 `TB`（launcher.dcl + tb-main.lsp）

### 1.1 ESC 关闭（⭐ 最高优先，本次唯一逻辑改动）

**背景**：P1 移除了每页 `cancel_button`，LSP 新增 `(< result 0)` 分支兜底。不同版本对"无 is_cancel 按钮时按 ESC"返回值不同（0 或 -1），需实测。

| 操作 | 预期 |
|---|---|
| 打开 `TB` → 按 Esc | 主界面**关闭**，命令行回命令行提示符 |
| 打开 `TB` → 点"关闭"按钮 | 主界面关闭 |
| 打开 `TB` → 点"设置(S)" → 关设置 → 回到主界面 | 正常返回 |

**预案**：若按 Esc 无反应（对话框不关闭也不重弹）→ 该版本 ESC 被忽略，可将"关闭"按钮的 `is_default=true` 改为 `is_cancel=true`（主界面无 Enter 快捷操作，Enter 意义小，Esc 更重要）。改前先确认点"关闭"仍正常。

### 1.2 initial_focus 每页跟随

| 操作 | 预期 |
|---|---|
| 打开 `TB` → 用 Tab 键切到页 5（结构通用）→ 关闭再重开 | 每次打开，键盘初始焦点都在**当前页对应 tab** 上（页1→文字处理、页2→图层管理…页6→辅助功能），且该 tab 高亮 |
| 在任一页按一次 Enter（焦点在 tab 上） | 不切页（或切到自身重开），不崩溃 |

### 1.3 布局

| 项 | 操作 | 预期 |
|---|---|---|
| 缩放区 3 列 | 页 0 查看"快捷缩放·旋转"分区 | 3 列：缩放 0.5/2/4 × 缩放 5/100/1000 × 顺/逆旋转 4 个；按钮完整、无截断 |
| 对话框总宽 | 页 0 整体 | 完整显示，右侧无被屏幕截断（3 列使该分区比原来宽约 14 字符） |
| 宽度统一 | 页 4 标注区 / 页 5 钢筋编辑 / 页 6 打印区 | 同列按钮右缘对齐 |
| × 字形 | 页 0 缩放按钮 | "缩放0.5× S1"等 ×（乘号 U+00D7）正常显示，非方块/缺字 |
| CX 移组 | 页 0 "修改"分区点"选线修剪 CX" | 正常执行（key 未变，绑定仍有效） |
| 无 cancel_button | 页 0 底部 | 只有"设置(S)/帮助(H)/关闭"，无"取消" |

**预案**：× 显示为方块 → 老字体缺字形，回退为字母 `x` 或去掉（低优先）。

---

## 二、批量打印 `BPT`（batchprint.dcl + tb-mod-batchprint.lsp）

### 2.1 排序下拉（P2-12 合并）

| 操作 | 预期 |
|---|---|
| `BPT` → 检测图框（≥4 张图纸，最好跨 2 行）→ 右侧"排序方式"下拉选"X排序（左到右）"→ 点"排序" | 列表按 X 升序，status 显示"已按所选方式排序" |
| 同上选"Y排序（上到下）" | 按 Y 降序（上到下） |
| 选"蛇形排序" | 半图框高分行 + 偶数行反转；与旧三按钮行为一致 |
| 排序后点"预览"/"开始打印" | 使用排序后的 drawings |

**预案**：`(nth ... '(X Y S))` 若参数名错（'X/'Y/'S vs 代码内部对 snake 的判断），会报函数错误或在命令行提示参数类型；对照 `bp:sort-drawings` 定义（tb-mod-batchprint.lsp 第 282 行）核对 method 分支。

### 2.2 其余本次改动

| 项 | 操作 | 预期 |
|---|---|---|
| 纸张下拉 | 纸张列表查看 canonical 名显示 | 比旧版多显示 4 字符；切换打印机后仍刷新正常 |
| 对话框总宽 | 图框识别行 | 低分辨率（1024×768）下最右"拾取<<"完整可见 |
| 按钮列 | 图纸列表右侧 8 个控件与列表高度 | 视觉平衡，无按钮超出对话框 |

---

## 三、设置对话框 `TBSETTING`（setting.dcl + tb-main.lsp）

| 操作 | 预期 |
|---|---|
| 打开 `TBSETTING` | 光标初始在"出图比例 1:"框（initial_focus） |
| "云线颜色"输 `0` → 保存 | alert 弹出"云线颜色须为 1-255"，焦点回颜色框，**不保存** |
| 输 `256` → 保存 | 同上 |
| 输 `abc` → 保存 | 同上（`atoi "abc"`=0，走校验） |
| 留空 → 保存 | 同上（`atoi ""`=0） |
| 输 `1` 和 `255` → 保存 | 正常保存，关闭；重开显示保留值 |

**预案**：alert 弹出但焦点未回位 → 检查 `(mode_tile "cloud_col" 2)` 在字符串里是否生效。

---

## 四、回归确认（本次改动不应影响的）

| 项 | 预期 |
|---|---|
| 主界面 125 命令按钮全部 key 保留 | 布局改动只动 position/width，各页原有按钮点击均正常 |
| 批量打印整体流程 | 与「当前任务详情」P0 项一致：检测→排序→预览→打印四格式 |
| 设置其余字段保存 | 比例/文字样式/图层名等原保存逻辑不受 save 字符串加 if 影响 |
| ESC 在其他对话框 | 批量打印、设置对话框**保留** cancel_button，ESC 行为不变 |

---

## 五、建议执行顺序

1. 前置准备（加载 + 三个 DCL 能弹出）
2. **主界面 ESC（1.1）** — 逻辑改动，最先验
3. 设置校验（三）— 逻辑改动，其次
4. 排序下拉（2.1）— 逻辑改动
5. 其余纯布局项（1.2/1.3/2.2）— 低风险，可批量扫
6. 回归（四）

每完成一项在表中勾选；发现异常记录**版本 + 复现步骤 + 现象**，便于回改。
