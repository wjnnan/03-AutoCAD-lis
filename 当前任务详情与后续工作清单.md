# 当前任务详情与后续工作清单

> 本文件记录 2026-08-15 会话的完整工作，供后续会话/审查方/实机测试无缝接手。
> 关联记忆：`tb-guard-activation.md`（稳健性补强）、`bp-activex-rewrite.md`（批量打印重写+审核）。

---

## 一、任务背景

项目是 AutoCAD AutoLISP 工具箱 **TB-Toolbox**（约 6800 行、125 命令），核心交付物为 `TB-Toolbox/`（31 文件）+ `unified-lib/`（统一核心库）。本次会话完成三件事（按时间顺序）：

1. **横向评测 + 稳健性补强**（激活闲置的守卫框架）
2. **批量打印模块彻底重写**（`-PLOT` → ActiveX）
3. **批量打印审核**（整个程序背景下交叉审查，修复 6 bug）

---

## 二、已完成工作

### 2.1 横向评测 + 稳健性补强（87 命令套守卫）

**评测结论**：三维度（本代码现状 / 仓库内对标库 Lee Mac·@lisp·SyncBlock·DiffCheck / 联网外部最佳实践）一致指向——TB-Toolbox 的守卫框架 `uc:guard-*` 早已实现却 **0 调用**，约 120 命令无 `*error*`、无系统变量恢复、无 undo 分组。

**改动**：
- 阶段1：修复 `unified-lib/uc-core.lsp` 的 `uc:snapshot-sysvars`（`getvar` 加 `vl-catch-all-apply`）
- 阶段2：5 处系统变量快照恢复（`c:tq`→CLAYER、`c:tssd`→TEXTSTYLE、`c:ff`/`c:fr`→FILLETRAD、`c:nn`→OSMODE）
- 阶段3：旗舰命令守卫（钢筋 17 命令、选择易 `c:ss`/`c:sss`、批量打印 `c:BPT`）+ DCL 泄漏修复（`vl-filename-mktemp`→`TEMPPREFIX`、`dcl-f` 泄漏）
- 阶段4：其余多步命令铺开（62 命令）
- **总计 87 命令套守卫**，单字母快捷命令（`c:q`/`c:s`/`c:C1` 等 command 转发）不套

**验证**：17 文件括号平衡 0、守卫配对 87=87、快照正确、无手写 `*error*` 冲突。**未做实机验证。**

### 2.2 批量打印模块重写（`-PLOT` → ActiveX v2.0）

**改动文件**：
- `TB-Toolbox/tb-mod-batchprint.lsp`（重写 841 行，32 个 `bp:*` 函数 + `c:BPT`/`c:BPSET`）
- `TB-Toolbox/tb-dcl-batchprint.dcl`（重写，新增「蛇形排序」按钮）
- `TB-Toolbox/tb-lib-entity.lsp`（补强 `entity:block-def-bbox`/`entity:block-bbox-pure` 纯 Lisp 块包围盒）

**核心选型**（联网调研 BPDF/PI-Prot-On/BatchPlotPlus）：
- 打印引擎：ActiveX `vla-PlotToFile`（有 COM）+ `-PLOT` 兜底（无 COM）
- 图框识别：块(INSERT) + 闭合 4 顶点多段线 + 布局 三通道
- 输出格式：打印机名驱动（`DWG To PDF.pc3`=PDF、`DWF6 ePlot.pc3`=DWF、实体打印机=纸/PLT）
- 排序：X/Y 蛇形（半图框高容差分行 + 偶数行反转）
- 命名：图框属性 `图号-图名` 优先，回退 `图纸_序号`（零填充 3 位）
- 颜色：经 CTB（monochrome/Grayscale/acad）控制
- 纸张/打印机：动态枚举 `vla-GetPlotDeviceNames`/`GetCanonicalMediaNames`

### 2.3 批量打印审核（6 bug 修复）

| # | Bug | 修复 |
|---|---|---|
| 1 | 蛇形排序行内反序（cons 前插） | 分组改 `append + cdr` |
| 2 | 布局打印 `vla-item` 在 catch 参数列表提前求值 | 单独 catch |
| 3 | 配置回填对非 popup tile 误设 "0" | 加 `bp:tile-list` 判断 |
| 4 | 纸张名 canonical vs 标准名不匹配 | 统一 canonical 名 + `*BP:CURRENT-PAPERS*` |
| 5 | 默认打印机与 output_mode 错位 | init-dialog 默认选 PDF 打印机 |
| 6 | ActiveX 反向打印未实现 | `(if updown (setq rot (+ rot 2)))` |

---

## 三、当前状态与验证情况

**已验证（静态）**：
- 所有改动 .lsp 括号平衡（净深度 0）
- 守卫配对 87=87；批量打印 34 函数、11 个 ActiveX API 名称齐全
- DCL↔LSP tile 双向一致、无跨文件 `bp:*` 引用残留
- `c:BPT`/`c:BPSET` 命令名不变（主界面按钮绑定无需改）

**未验证（阻塞交付，需实机）**：
- 沙箱无 AutoCAD，ActiveX 的实际行为、`-PLOT` 兜底序列、守卫的 undo/ESC 行为**均未实机测试**

---

## 四、后续工作清单（按优先级）

### P0 — 实机验证（阻塞项，最高优先）

在 AutoCAD 2004 与最新版分别验证：

1. **批量打印核心场景**（`c:BPT`）：
   - [ ] 多图框逐帧输出：每页是对应图框窗口，**不重复、不整图输出**
   - [ ] 切换打印机后纸张下拉正确刷新，打印用纸正确
   - [ ] 布局打印（`include_layouts`）：一个布局一页，不崩溃
   - [ ] 比例自动匹配（AUTO）与自定义比例（CUSTOM 1:N）正确
   - [ ] 蛇形排序（新增 `btn_sort_s`）顺序正确
   - [ ] 命名零填充（图框有属性→`图号-图名`；无属性→`图纸_001`）
   - [ ] PDF/DWF/PLT/直接打印四种格式分别出图
   - [ ] 颜色模式（黑白/灰度/彩色）经 CTB 生效
   - [ ] 反向打印生效
2. **ActiveX 参数值核对**（静态只能保证 API 名正确，值需实机确认）：
   - [ ] `acWindow`/`acLayout` 枚举值（代码用 `boundp` 探测 + 默认 4/5，需确认各版本实际值）
   - [ ] `vla-put-canonicalmedianename` 接受 `GetCanonicalMediaNames` 返回的字符串格式
   - [ ] `vla-SetCustomScale layout 1.0 N` 的 1:N 语义
3. **稳健性补强的守卫行为**（87 命令）：
   - [ ] undo 分组：多步命令一次撤销（`UC` 一次回到命令前）
   - [ ] ESC 中断：系统变量恢复、无残留临时状态
   - [ ] ZWCAD 无 COM：`uc:undo-begin` 走 `command "_.UNDO" "_BEGIN"` 兜底正常

### P1 — 降级路径验证（无 COM 平台）

- [ ] ZWCAD/中望下 `-PLOT` 兜底的纸张名格式（标准名 vs ISO 名，需实测调整）
- [ ] ZWCAD 下块图框识别（`entity:block-bbox-pure` 纯 Lisp 递归）对嵌套块/旋转块的精度

### P2 — 遗留问题（不影响崩溃，但需决策）

| 问题 | 位置 | 建议 |
|---|---|---|
| `-PLOT` 兜底纸张名格式不确定 | `bp:plot-command` | 实机后统一为 ISO 名 |
| `bp:get-papers` 回退用标准名（ActiveX 枚举失败时） | `bp:get-papers` | 回退也补 ISO 映射 |
| `action_tile` 字符串内全局变量泄漏（`p`/`pl`/`cfg` 等） | `c:BPT` | 既有模式，可选清理 |
| `bp:execute-print` 冗余局部变量（`printer`/`paper` 等声明未用） | `bp:execute-print` | 清理 |
| `c:ss` 的 `new_dialog` 失败未补 `unload_dialog`（稳健性补强暂缓项） | `tb-mod-select.lsp` | 触发概率极低，可选修复 |
| 测试脚本失效 | `tests/verify_*.py` | GBK 编码导致 UnicodeDecodeError，需改 `encoding='gbk'` |

### P3 — 可选优化（非必需）

- [ ] 纸张下拉显示本地化中文名（`GetLocaleMediaName`），存储 canonical 名
- [ ] PDF 合并改用 Ghostscript（比 pdftk 更通用）
- [ ] 批量打印配置持久化复用 `sys:*` 体系（当前用独立 cfg 文件）

---

## 五、关键注意事项（接手提示）

1. **GBK 编码**：`TB-Toolbox/` 与 `unified-lib/` 源码是 GBK 编码（无 BOM，2004 兼容），`Edit` 工具按 UTF-8 读会乱码中文。改这些文件必须用 Python `io.open(path, encoding='gbk')` 读写，或只改 ASCII 代码行。
2. **守卫框架**：87 命令已套 `uc:guard-begin/end`。`c:TB`/`c:TBSETTING` **故意未套**（保留含 `unload_dialog` 的手写 `*error*`，DCL 场景正确模式）。新增命令若改系统变量，需在 `guard-begin` 快照清单里声明。
3. **批量打印引擎分派**：`bp:plot-one` 内 `(if (and *SYS:HAS-ACTIVEX* (uc:com-available-p)) ActiveX 否则 -PLOT)`。ActiveX 是主路径，`-PLOT` 是降级。
4. **实机验证脚本**：可在 `TB-Toolbox/tb-mod-batchprint.lsp` 加载后，命令行逐条测 `c:BPT`（或 `c:BPSET`），观察图纸列表与输出的 PDF/DWF 文件。
